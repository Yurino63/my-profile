Part 1：AIと考えた1セッション

テーマ

Reactで作成したweek14の書籍紹介ページのコードについて、AIにレビューしてもらった。

AIへのプロンプト

おすすめ本を紹介するページを作っています。

BookCardコンポーネントを別ファイルに作成し、
title、author、rating、commentをpropsで渡しています。
また、書籍データを配列で管理してmapでBookCardを表示しています。

この実装について、改善点や、なぜその書き方をするのかを説明してください。コードを修正する場合は、理由も説明してください。

AIからの回答

BookCardを別コンポーネントに分けることで、
1冊分の表示処理をまとめることができる。

また、propsを使うことで、同じBookCardコンポーネントに
本ごとに異なるtitle、author、rating、commentを渡すことができる。

書籍データを配列にしてmapを使うことで、
同じBookCardを繰り返し表示できる。

「なぜ？」と聞いたこと

なぜ本の情報をBookCardの中に直接書かずに、
propsで渡す必要があるのですか？

「なぜ？」を聞いて新しく分かったこと

propsを使うことで、BookCardは本の具体的な内容を持たず、
外から渡されたデータを表示するだけの再利用できるコンポーネントになることが分かった。

また、本が増えた場合でも、同じBookCardをmapで使い回せることが分かった。

出力の4分類

採用

BookCardにtitle、author、rating、commentをpropsとして渡す方法。

理由：今回の課題の要件に合っていて、本が増えても同じコンポーネントを使えるため。

保留

評価を星5個で表示する方法。

理由：今回の実装では使えるが、評価の表示方法には他の方法もあるため。

採用しない

BookCardの中に本のタイトルや著者などを直接書く方法。

理由：本が増えるたびにコードを追加する必要があり、mapを使って一覧表示する今回の目的に合わないため。

要確認

Tailwind CSSのクラスをAIの回答だけを見てそのまま追加すること。

理由：自分の環境で実際に動作するか確認する必要があるため。

Part 2：流された場面の振り返り

Week9で、AIが提案したコードを仕組みを十分に理解しないまま採用したことがあった。
コードが動いたため、その理由を確認せずにそのまま使ってしまった。

Part 3：今後のAI利用方針

Week11を通して、AIのコードをそのまま使うのではなく、なぜその書き方になるのかを理解することが大切だと分かった。
今後は、AIからコードを提案されたとき、分からない部分について最低1回は「なぜ？」と聞いてから採用する。
